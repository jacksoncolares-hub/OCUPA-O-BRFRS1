# BRFRS1 Occupancy Center — versão final

## Antes de publicar
Abra `config.js` e cole a URL do Apps Script terminada em `/exec`.

## GitHub
```cmd
git add .
git commit -m "Instala versão final BRFRS1 Occupancy Center"
git push origin main
```

Abra:
https://jacksoncolares-hub.github.io/OCUPA-O-BRFRS1/heatmap.html

Use Ctrl + F5.
