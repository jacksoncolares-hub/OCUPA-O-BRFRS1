window.APP_CONFIG = {
  // Fonte oficial publicada pelo Apps Script no próprio repositório.
  // Não use a URL privada do Apps Script no navegador.
  SHEET_API_URL: "",
  // Atualiza automaticamente a cada 15 minutos.
  REFRESH_MS: 900000,
  DEFAULT_ZONE: "B",

  // Se true, o Excel importado manualmente continua sendo usado ao reabrir o navegador.
  REMEMBER_IMPORTED_EXCEL: true
};
