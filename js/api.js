
window.WMS=(()=>{let cache=null;const c=window.APP_CONFIG||{};async function load(force=false){if(cache&&!force)return cache;const u=(c.SHEET_API_URL||'').trim();const src=u?`${u}${u.includes('?')?'&':'?'}${force?'nocache=1&':''}t=${Date.now()}`:'data.json';const r=await fetch(src,{cache:'no-store'});if(!r.ok)throw new Error(`HTTP ${r.status}`);const d=await r.json();if(d.ok===false)throw new Error(d.error||'Erro da API');d.zones=Array.isArray(d.zones)?d.zones:[];d.corridors=Array.isArray(d.corridors)?d.corridors:[];d.cells=Array.isArray(d.cells)?d.cells:[];d.meta=d.meta||{};d.overall=d.overall||{};cache=d;return d}function zones(d){const s=new Set();d.zones.forEach(x=>s.add(String(x.Zona)));d.cells.forEach(x=>s.add(String(x.Zona)));return[...s].sort((a,b)=>a.localeCompare(b,undefined,{numeric:true}))}function cls(v){if(v==null||Number.isNaN(Number(v)))return'blocked';v=Number(v);return v>=90?'critical':v>=75?'alert':v>=40?'healthy':'idle'}function fmt(v){return new Intl.NumberFormat('pt-BR').format(Number(v||0))}return{load,zones,cls,fmt,config:c}})();
