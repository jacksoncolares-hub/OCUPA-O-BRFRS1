let scene,camera,renderer,group,zone='A',level=1,rot=0;function box(x,y,z,w,h,d,c){const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),new THREE.MeshLambertMaterial({color:c}));m.position.set(x,y,z);group.add(m)}function build(){if(group)scene.remove(group);group=new THREE.Group();scene.add(group);if(zone==='A'){for(let r=1;r<=50;r++){const x=-42+(50-r)*1.65,v=usable(corridor('A',r));box(x,1,20,.7,2,35,0x25292d);box(x,.15,20,.78,.15,35,new THREE.Color(heatColor(v)).getHex())}}else{for(let r=1;r<=13;r++){const x=-20+r*3.4,v=usable(corridor('B',r));for(let n=1;n<=7;n++)box(x,n*1.8,15,1.2,.18,34,0xee6b21);box(x,6.5,15,.7,13,34,new THREE.Color(heatColor(v)).getHex())}}}function init(){const el=$('#viewer');scene=new THREE.Scene();scene.background=new THREE.Color(0x252b31);camera=new THREE.PerspectiveCamera(50,el.clientWidth/el.clientHeight,.1,500);renderer=new THREE.WebGLRenderer({antialias:false});renderer.setPixelRatio(1);renderer.setSize(el.clientWidth,el.clientHeight);el.prepend(renderer.domElement);scene.add(new THREE.HemisphereLight(0xffffff,0x555555,1.5));const floor=new THREE.Mesh(new THREE.PlaneGeometry(150,120),new THREE.MeshLambertMaterial({color:0x999999}));floor.rotation.x=-Math.PI/2;scene.add(floor);build();animate();renderer.domElement.onwheel=e=>camera.position.multiplyScalar(e.deltaY>0?1.08:.92);let drag=false,lx=0;renderer.domElement.onpointerdown=e=>{drag=true;lx=e.clientX};onpointerup=()=>drag=false;renderer.domElement.onpointermove=e=>{if(drag){rot+=(e.clientX-lx)*.006;lx=e.clientX}}}function animate(){requestAnimationFrame(animate);camera.position.set(Math.sin(rot)*85,55,Math.cos(rot)*85);camera.lookAt(0,4,20);renderer.render(scene,camera)}async function start(){await loadData();init();$('#zoneSelect').onchange=e=>{zone=e.target.value;build()};$('#levelSelect').innerHTML=Array.from({length:7},(_,i)=>`<option>Nível ${i+1}</option>`).join('');$('#searchBtn').onclick=()=>toast('Pesquisa disponível no próximo ajuste')}start();
